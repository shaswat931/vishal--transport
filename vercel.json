{
  "version": 2,
  "builds": [
    { "src": "vishal--transport/**/*", "use": "@vercel/static" },
    { "src": "backend/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/$1" },
    { "src": "/(.*)", "dest": "vishal--transport/$1" }
  ]
}
